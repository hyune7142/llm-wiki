# LLM Wiki 스키마

이 파일은 Claude Code가 위키를 어떻게 유지·관리할지 정의하는 핵심 설정 파일입니다.
`/wiki-init`을 실행하면 아래 [미설정] 항목들이 채워집니다.

---

## 위키 기본 정보

- **위키 이름**: [미설정]
- **도메인 / 주제**: [미설정]
- **목적**: [미설정]
- **주요 소스 유형**: [미설정]
- **강조 관점 / 정리 방식**: [미설정]

---

## 디렉토리 구조

```
/
├── raw/              # 원본 소스 (Claude가 읽기만 함, 절대 수정 금지)
│   └── assets/       # 이미지, 첨부파일 등
├── wiki/             # Claude가 생성·유지하는 위키 페이지
│   ├── index.md      # 전체 페이지 카탈로그 (Claude가 항상 최신 상태 유지)
│   ├── log.md        # 작업 로그 (append-only)
│   └── overview.md   # 전체 합성 요약 (선택)
├── CLAUDE.md         # 이 파일 — 스키마 및 워크플로우 정의
└── README.md         # 사람이 읽는 사용 방법 안내
```

**규칙**:
- `raw/` 안의 파일은 절대 수정하지 않는다.
- `wiki/` 안의 모든 파일은 Claude가 작성하고 유지한다. 사용자는 읽기만 한다.
- `wiki/index.md`와 `wiki/log.md`는 모든 작업 후 반드시 업데이트한다.

---

## 페이지 형식

모든 위키 페이지는 YAML 프론트매터로 시작한다:

```yaml
---
title: 페이지 제목
type: [source | entity | concept | comparison | query | overview]
tags: []
sources: []          # 이 페이지가 참조한 raw/ 파일 목록
updated: YYYY-MM-DD
---
```

**타입 설명**:
- `source` — 원본 소스 하나의 요약 페이지
- `entity` — 인물, 기관, 제품 등 특정 개체 페이지
- `concept` — 주제, 개념, 아이디어 페이지
- `comparison` — 여러 소스 / 개체를 비교한 페이지
- `query` — 사용자 질문에 대한 답변을 보존한 페이지
- `overview` — 전체 합성 요약

**링크 규칙**: 위키 내 다른 페이지는 항상 `[[페이지명]]` 형식으로 연결한다.

---

## 워크플로우

### Ingest (소스 추가)

사용자가 새 소스를 `raw/`에 넣고 처리를 요청하면:

1. 소스를 읽고 핵심 내용을 파악한다.
2. 사용자와 주요 인사이트를 간단히 논의한다.
3. `wiki/` 에 소스 요약 페이지(`type: source`)를 작성한다.
4. 관련된 기존 entity / concept 페이지를 찾아 업데이트하거나 새로 생성한다.
5. `wiki/index.md`에 새 페이지를 등록한다.
6. `wiki/log.md`에 ingest 항목을 추가한다. 형식: `## [YYYY-MM-DD] ingest | 소스 제목`

### Query (질문 답변)

사용자가 질문하면:

1. `wiki/index.md`를 읽고 관련 페이지를 파악한다.
2. 관련 페이지들을 읽고 답변을 합성한다.
3. 답변에 위키 페이지 출처를 명시한다.
4. 답변이 재사용할 만한 분석이면 `wiki/`에 `type: query` 페이지로 저장하고 index를 업데이트한다.
5. `wiki/log.md`에 query 항목을 추가한다. 형식: `## [YYYY-MM-DD] query | 질문 요약`

### Lint (위키 점검)

사용자가 위키 점검을 요청하면:

1. 모든 위키 페이지를 순회하며 아래 항목을 확인한다:
   - 페이지 간 모순되는 주장
   - 더 최신 소스로 대체되어야 할 낡은 내용
   - 인바운드 링크가 없는 고아 페이지
   - 언급은 되었지만 전용 페이지가 없는 개념
   - 누락된 크로스 레퍼런스
2. 발견된 문제를 보고하고, 개선 우선순위를 제안한다.
3. 추가로 조사하면 좋을 질문이나 소스를 제안한다.
4. `wiki/log.md`에 lint 항목을 추가한다. 형식: `## [YYYY-MM-DD] lint | 요약`

---

## 커스터마이징 가이드

아래 항목들은 도메인에 따라 자유롭게 수정하세요:

- **페이지 타입**: 필요한 타입을 추가하거나 불필요한 타입을 제거
- **프론트매터 필드**: 도메인에 맞는 메타데이터 필드 추가 (예: `author`, `date`, `rating`)
- **Ingest 워크플로우**: 논의 단계를 생략하고 자동 처리하도록 변경 가능
- **출력 형식**: 마크다운 외에 Marp 슬라이드, 표, 차트 등을 기본 출력으로 지정 가능
- **로그 형식**: 팀 협업이면 작성자 필드 추가 가능

---

*이 파일은 `/wiki-init`으로 초기 설정하고, 이후 대화를 통해 Claude와 함께 발전시켜 나가세요.*
